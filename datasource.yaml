datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://prometheuschart-kube-prome-prometheus.prometheus.svc.cluster.local:9090
      access: proxy
      isDefault: true
